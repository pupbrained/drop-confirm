{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${mod_version}",
  "description": "${mod_description}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.example.mymod.FabricEntrypoint"
    ]
  }
}
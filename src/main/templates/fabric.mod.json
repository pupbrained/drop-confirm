{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${mod_version}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "environment": "client",
  "icon": "drop_confirm.png",
  "entrypoints": {
    "main": [
      "xyz.pupbrained.drop_confirm.loaders.fabric.FabricEntrypoint"
    ],
    "modmenu": [
      "xyz.pupbrained.drop_confirm.loaders.fabric.ModMenuIntegration"
    ]
  },
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": ">=${minecraft_version}",
    "java": ">=${java}",
    "fabric-api": ">=${fabric_api}",
    "fabric-language-kotlin": "*",
    "${config_lib}": "${config_lib_version}"
  }
}
